export const environment = {
  production: false,
  apiUrl: 'http://localhost:3005/api',
  /** Origen donde se sirven `/uploads/...` (mismo host que la API en desarrollo). */
  uploadsPublicOrigin: 'http://localhost:3005'
};
